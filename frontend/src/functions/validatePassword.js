const password = "P@ssw0rd!";

function validatePassword(password) {
  const input = password;

  if (input >= 8) console.log("Password at least 8 characters");
  else console.log("Passwords needs to be at least 8 characters");
}
